#include "main.h"


/**
 * mul - multipies two integers
 * @x: The first number to multiply
 * @y: The second number to mult wth first no
 * Return: The result of multiplication between x parameter and y
 */

int mul(int x, int y)

{

	return (x * y);
}
